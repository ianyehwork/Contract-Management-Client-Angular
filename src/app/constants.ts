import { NgbModalOptions } from "@ng-bootstrap/ng-bootstrap";

/**
 * These are the constants for the application
 */
export class AppConstants {
    public static readonly MODAL_OPTIONS: NgbModalOptions = {size: 'lg', centered: true, backdrop : 'static'};
}
